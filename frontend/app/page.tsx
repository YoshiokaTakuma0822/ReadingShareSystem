import HomeScreen from './screens/HomeScreen'

/**
 * Page component for the root route of the application.
 *
 * This page renders the main `HomeScreen` component.
 *
 * @returns A React element representing the home screen.
 */
export default function Page() {
    return <HomeScreen />
}
